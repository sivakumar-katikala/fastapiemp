# NOTE ON DATABASE: this project uses ASYNC SQLAlchemy with a real
# PostgreSQL server via the `asyncpg` driver (fast, native async Postgres
# driver). `psycopg2-binary` is also included for the standalone
# `seed_data.py` script, which uses plain sync SQLAlchemy for simplicity.
# PostgreSQL must be installed and running separately -- see README.md.
# Versions are pinned to releases that ship prebuilt wheels for Python 3.13
# on Windows/macOS/Linux, so `pip install` never needs a C/C++ compiler.
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
greenlet==3.1.1
# NOTE: SQLAlchemy's async engine (AsyncSession/create_async_engine) requires
# the `greenlet` package at runtime to bridge async/await with its internal
# sync core. It's normally pulled in automatically as a transitive dependency,
# but is pinned explicitly here since some environments (e.g. newer Python
# versions without a prebuilt greenlet wheel resolving correctly) can end up
# missing it, causing "ValueError: the greenlet library is required..." on
# startup.
asyncpg==0.30.0
psycopg2-binary==2.9.10
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
# NOTE: passlib 1.7.4's bcrypt backend is incompatible with bcrypt>=4.0
# (bcrypt 4.x removed the `__about__.__version__` attribute passlib checks,
# and also enforces a strict <=72-byte secret limit that breaks passlib's
# internal self-test). bcrypt is pinned to 3.2.2, the last version that
# works correctly with passlib 1.7.4. Do not upgrade bcrypt alone.
python-multipart==0.0.9
jinja2==3.1.4
python-dotenv==1.0.1
email-validator==2.2.0
